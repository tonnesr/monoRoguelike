namespace mono2.src.models 
{
  public enum Direction {
    Up,
    Down,
    Right,
    Left
  }

  public enum EnemyType { // NOTE could change to more classes that inherits from enemy
    Monster
  }
}